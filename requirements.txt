# Install the CUDA-enabled PyTorch build separately, e.g. from the official
# PyTorch CUDA index. Do not replace a working +cu126 installation with a
# default CPU-only wheel.
numpy>=1.26
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.8
psutil>=5.9
tqdm>=4.66
